import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#6c5dd4",
    },
    secondary: {
      main: "#1c768f",
    },
    error: {
      main: "#f7444e",
    },
  },
});

export default theme;
